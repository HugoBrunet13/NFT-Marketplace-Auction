const { expect } = require('chai');
const { ethers } = require('hardhat');




describe('NFT Collection contract', () => {


        it('Tests ', async () => {
            expect(0).to.equal(0);
        })

})